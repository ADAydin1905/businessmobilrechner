/* ---------------------------------------------------------------------------
 * gitlab-config.js  (Stub für GitHub-Pages-Hosting)
 *
 * Diese Datei wird von der HTML referenziert. Beim Hosting über GitHub Pages
 * gibt es KEINE Live-Speichern-Funktion (statischer Host, kein Server).
 * Der Stub verhindert lediglich einen 404-Fehler in der Browser-Konsole.
 *
 * Das Admin-Panel "Download"-Pfad funktioniert weiterhin: geänderte
 * tarife.json / rabatte.json herunterladen und manuell ins Repo committen.
 *
 * Wenn du später echtes Live-Speichern willst (GitLab oder AWS), hier die
 * Konfiguration eintragen.
 * ------------------------------------------------------------------------- */
window.GITLAB_CONFIG = {
  apiUrl: "",
  projectId: "BITTE_AUSFUELLEN",
  branch: "main",
};
